import { describe,expect,it } from 'vitest';import { forecastBalance,goalProgress,totalBalance,totals } from './finance';import type { Account,Goal,Transaction } from '../types/finance';
const base={date:'2026-08-01',accountId:'a',categoryId:'c',status:'paid' as const,createdAt:'2026-08-01'};
describe('finance rules',()=>{it('does not count transfers',()=>{const items:Transaction[]=[{...base,id:'1',description:'Receita',type:'income',amount:1000},{...base,id:'2',description:'Despesa',type:'expense',amount:400},{...base,id:'3',description:'Transferência',type:'transfer',amount:300}];expect(totals(items)).toEqual({income:1000,expense:400,result:600});});it('only active accounts',()=>{const a:Account[]=[{id:'1',name:'A',institution:'X',type:'checking',balance:100,color:'#000',active:true},{id:'2',name:'B',institution:'X',type:'checking',balance:900,color:'#000',active:false}];expect(totalBalance(a)).toBe(100);});it('goal ceiling',()=>{const g:Goal={id:'1',name:'Meta',current:120,target:100,deadline:'2027-01-01',icon:'target'};expect(goalProgress(g)).toBe(100);});it('pending projection',()=>{const due=new Date();due.setDate(due.getDate()+3);const a:Account[]=[{id:'a',name:'A',institution:'X',type:'checking',balance:1000,color:'#000',active:true}];const t:Transaction[]=[{...base,id:'1',description:'Conta',type:'expense',amount:250,status:'pending',dueDate:due.toISOString().slice(0,10)}];expect(forecastBalance(a,t,30)).toBe(750);});});
